# Third-party notices

- Inter4.1: bundled font files and [SIL Open Font License](packages/beds/fonts/OFL.txt).
- Geist Mono: bundled font and [SIL Open Font License](packages/beds/fonts/Geist-OFL.txt).
- Lucide: runtime dependency, its upstream ISC license applies; no copied alternate icon library.
- Better skills: referenced at a pinned MIT-licensed upstream revision in [Interface quality](docs/design/espaco-library/INTERFACE-QUALITY.md). Skill implementations are not silently bundled or installed here.
- Marc Lou landing-page skill: BEDS-authored adaptation of the project's existing paraphrased Marc heuristics; [scope and attribution](packages/beds/skills/marc-lou-landing-page/SKILL.md). Not official or endorsed; no full article text or third-party artwork bundled. Original-post fidelity remains unverified, as documented in the skill.
- beUI (`starc007/ui-components`): MIT-licensed animated React components consulted as the first source for any component work in BEDS. Adopted per [External component sourcing](docs/design/espaco-library/EXTERNAL-COMPONENT-SOURCING.md): logic, state model, motion and Tailwind class strings are pasted intact and become BEDS-owned; only geometry classes are swapped for BEDS proportions. No runtime import of beUI; beUI Pro blocks are unlicensed here and excluded. Each adopted component records slug, source URL and retrieval date in this file.
  - `AnimatedNumber` ← beUI `number`, `https://beui.dev/r/number/raw`, retrieved 2026-09-16, MIT. Upstream Tailwind classes, `cn` helper and easing module dropped; motion intent re-expressed with BEDS easing and tokens. F3 wires it to `AccountCredits`, typed `Metric` values and controlled `PricingCard` amount changes; localized labels are never parsed.
  - Text field focus ring and error nudge ← beUI `input`, `https://beui.dev/r/input/raw`, retrieved 2026-09-16, MIT. Only the state intent was adopted (soft focus ring instead of a detached outline, one nudge on a new error, settled error message). Upstream pill geometry, 44px height, Tailwind `ring-*` classes, imperative `motion.animate` calls and animated success check were not adopted: BEDS keeps its own field geometry and expresses this in CSS with `--es-focus-ring`/`--es-error-ring`.
  - `Button` / `IconButton` / `IconToggleButton` ← beUI `button-base`, `https://beui.dev/r/button-base/raw`, retrieved 2026-09-17, MIT. Motion (`whileTap` press scale, `whileHover` subtle lift via `useHoverCapable`, `motion.span` rotate spinner) and disabled idiom (`pointer-events-none` + `opacity-50`) adopted intact. Geometry swaps: `h-11 rounded-full px-5` → `min-h-8 rounded-lg px-2.5` (default), `min-h-7 rounded-md px-2.5` (compact), `min-h-10 rounded-[12px] px-3.5` (welcome — arbitrary radius, BEDS has no 12px token), `min-h-10 rounded-xl px-4` (connection). The upstream `icon` size was dropped in favor of BEDS compact 32×32 with `pointer-coarse:min-h-11 min-w-11`. `ButtonLink` / `MagneticButton` / `StatefulButton` / `MetallicButton` were not adopted.
  - `EASE_OUT` / `SPRING_PRESS` / `SPRING_SWAP` / `SPRING_PANEL` / `SPRING_LAYOUT` ← beUI `lib/ease.ts`, `https://beui.dev/r/button-base` (companion util), retrieved 2026-09-17, MIT. Mirrored to `packages/beds/src/lib/ease.ts`. `EASE_OUT` = `[0.16, 1, 0.3, 1]` matches the BEDS CSS custom property `--ease-out-expo`. Internal only, never exported.
  - `useHoverCapable` ← beUI `lib/hooks/use-hover-capable.ts`, `https://beui.dev/r/button-base` (companion util), retrieved 2026-09-17, MIT. Mirrored to `packages/beds/src/lib/hooks/use-hover-capable.ts`. Internal only, never exported.
  - `TextField` / `TextAreaField` / `SearchField` ← beUI `input`, `https://beui.dev/r/input/raw`, retrieved 2026-09-17, MIT. State intent adopted: error shake via `motion.animate` (one-shot x oscillation, reduced-motion guarded), settled error message via `motion.p` + `AnimatePresence`, soft focus ring replaces offset outline. Geometry swaps: `h-11 rounded-full` → `h-9 rounded-lg py-1 px-2.5` (settings, 36px) / `h-10 rounded-xl px-4` (connection, 40px preserved on touch) / `h-8 rounded-lg px-2.5` (search shell). Touch ergonomics inlined: `pointer-coarse:h-11 pointer-coarse:text-lg` (settings/search, 16px anti-zoom) and `pointer-coarse:h-10 pointer-coarse:text-lg` (connection, 40px preserved). The beUI animated success check and `motion.svg` path draw were not adopted: BEDS does not own a success state on fields. The beUI pill geometry, `ring-*` classes and upstream 44px default were not adopted.
  - `Checkbox` ← beUI `checkbox`, `https://beui.dev/r/checkbox/raw`, retrieved 2026-09-17, MIT. Check enter via `motion.span` + `AnimatePresence` (scale .6 → 1 in 160ms BEDS easing, reduced-motion guarded) and `whileTap` scale .92 on the visual box. Native `<input type="checkbox">` kept for form/value semantics; the beUI `motion.button role="checkbox"` was not adopted (would lose native form participation). Geometry preserved (16×16 box, `--radius-md` = 4px). Touch target grows to 44px on coarse pointer via `pointer-coarse:min-h-11`.
  - `Switch` ← beUI `switch`, `https://beui.dev/r/switch/raw`, retrieved 2026-09-17, MIT. Thumb travel via `motion.x` (0 → 14px in 180ms BEDS easing, reduced-motion guarded) instead of beUI's flexbox `justify-end`. Native `<input type="checkbox" role="switch">` kept for form/value semantics; the beUI `motion.button role="switch"` was not adopted. Geometry preserved (32×18.4 track, 16 thumb). Touch target grows to 44px on coarse pointer.
  - `SegmentedControl` ← beUI `radio`, `https://beui.dev/r/radio/raw`, retrieved 2026-09-19, MIT. **beUI modified**: BEDS keeps its monolithic controlled API and native `<input type="radio">` participation instead of the upstream compound `RadioGroup` / `RadioGroupItem` and `motion.button` role API. Upstream selected-surface motion intent is adapted with an instance-scoped `layoutId`, `.92` press scale, BEDS `SPRING_LAYOUT`/`SPRING_PRESS` and `useReducedMotion`; BEDS adds explicit ArrowLeft/Right (pill) and ArrowLeft/Right/Up/Down/Home/End (joined) navigation with focus transfer, disabled skipping and no Enter handler. Keyboard selection/reveal is instant and direction-aware in RTL; pointer/Space remain native, controlled `value` remains authoritative for checked/indicator state, and pill24px/joined36px geometry plus dark tokens are preserved. The 2px focus perimeter is painted inside the compact lane and maps to `Highlight` in forced-colors. Long labels use the BEDS overflow-contained inline lane with no page overflow; the existing hydration gate covers two instance-scoped layout groups without mismatch warnings. No runtime beUI import.
  - `Tabs` ← beUI `tabs`, `https://beui.dev/r/tabs/raw`, retrieved 2026-09-19, MIT. The monolithic BEDS API and native tab semantics remain; the beUI compound API and public styling/scroll escapes are not exposed. BEDS adapts its compatible `layoutId` indicator and panel reveal intent across activity, connection and settings, retaining BEDS geometry: activity compact/wrap, connection 16px shell/53px strip/36px list with 16/32/32px panel padding, and settings 4px lane/48px row/2px underline/24px panel gap. Pointer and edge-button changes may use `SPRING_LAYOUT` plus a 180ms `EASE_OUT` panel opacity/translateY(4px); ArrowLeft/Right/Home/End and Enter/Space proposals settle indicator and panel transforms instantly once the controlled value accepts them, including lag/rejection, and reduced motion is always instant. Native roving focus, disabled skipping, RTL settings arrows, ResizeObserver/font-ready/item remeasurement, bounded overflow, fade edges, 44px edge controls, unique IDs and forced-colors state stay BEDS-owned. Raw SHA-256 `7e3def75375631d855187fd39dcc4398e6f16313149d2be21375a2eae5a7d649`; registry JSON SHA-256 `486f271a0b4de568ba5382e1c5bd602a69ff19385c528460a506845e9a749803`.
  - `Dock` / `DockItem` / `DockSeparator` ← beUI `dock`, `https://beui.dev/r/dock/raw`, retrieved 2026-09-18, MIT. Pasted intact; `size` prop removed from public API (BEDS owns the 44px touch target). Uses `layoutId` for the active pill glide (`SPRING_LAYOUT`). Background uses `bg-card/80` + `backdrop-blur-xl`; no geometry overrides beyond removing the public `className`/`size` escape hatches.
  - `NumberTicker` ← beUI `number-ticker`, `https://beui.dev/r/number-ticker/raw`, retrieved 2026-09-18, MIT. Slot-machine rolling digits with staggered entry. Public `className`/`digitClassName` props removed per BEDS contract; component renders in inherited font metrics. Used for live metrics and counts — never for the fabricated ATS score, which remains static by invariant.
  - `ProcessingView` status marks and ATS segment fill ← beUI `todo-list`, `https://beui.dev/r/todo-list/raw` and registry item `https://beui.dev/r/todo-list.json`, retrieved 2026-09-19, MIT. Registry metadata identifies Saurabh Chauhan and dependencies `clsx`, `lucide-react`, `motion` and `tailwind-merge`; source raw SHA-256 `ee3b0baabf79fb941f0affbc21e9043c93cd02e59f71ed3f37d55702912cd210`; registry JSON SHA-256 `f247b5cc7c399e851ddf91660ddfa462c7edcf59ecbac01b6a7139d13e86758a`; repository `starc007/ui-components` main HEAD `90c29d7f80f661263f7b424629738e40a5a48db7`, MIT license SHA-256 `9e27b491d5691a1bd95708e79de23beb2962f33fe6c1ee31ab639f43009a0b20`. BEDS adapts the source status-mark swap intent and `motion/react` reduced-motion guard inside the existing numbered 28px ProcessingView tiles. The source collapsible list, completion counter, auto-collapse, detail model and public styling escapes are not exposed. ATS text remains the host-provided final value from first render; only the internal 28-segment fill reveals visually through a clipped motion layer, while the MeterSegments ARIA value remains final. No runtime beUI import or new dependency.
  - `Tooltip` plus `TooltipSurface` and helpers ← beUI `tooltip`, `https://beui.dev/r/tooltip/raw` and complete registry item `https://beui.dev/r/tooltip.json`, retrieved 2026-09-19, MIT. Full source set recovered without shadcn: `use-dismiss`, `use-hover-gesture`, `use-tap-gesture`, `touch`, and surface. Geometry/API adaptations: upstream trigger-controlled options and public `className`/`style` escapes are not exposed; BEDS keeps `label` + child, bottom default, r8 surface, 8px gap and mounts the fixed portal in `.es-root` for theme-token inheritance. Registry JSON SHA-256 `b4b154e9a3b5e5c20f258dd65ea18b4825e70fdf32d11da057ed4527fd9ec2ad`; raw SHA-256 `248e25c9e322f862ca2bf982d7a8747fe5bf0f779a1b009f34f522dc93c6bcb0`. Independent technical review and integrated verify are PASS_WITH_NOTES; historical mobile flake was not reproduced. Physical AT, non-Chromium and aesthetics remain pending.
  - `LoadingIndicator` ← beUI `loader`, `https://beui.dev/r/loader/raw`, retrieved 2026-09-19, MIT. Spinner geometry, motion/react and reduced-motion fallback adapted locally; BEDS label, status semantics and muted-foreground token retained. Registry JSON SHA-256 `0bd742a4322bfe6594ba1d9e21b83c99cf33c197d3bddb582db1d6679cd1bd97`; raw SHA-256 `24a3255b7a9b106cd60c246853864b541d9dd665c9fda99d314b74c98a01d66c`. Focused and integrated technical/artifact checks are PASS_WITH_NOTES; non-Chromium, physical AT and aesthetics remain pending.
  - `Toaster` / `toast` ← beUI `animated-toast-stack`, `https://beui.dev/r/animated-toast-stack/raw`, retrieved 2026-09-19, MIT. Source-comment provenance records SHA-256 `e1fe639d87cc5419b720e48289a9ee2ba776050413cede9b8f08af48ee28da28`; BEDS preserves its public event API, top-centered geometry, timeout/pause/live-region/focus contracts and adapts motion to the approved runtime/tokens. No separate recovery fragment was supplied; the local source comment is the provenance record. Focused and integrated technical/artifact checks are PASS_WITH_NOTES; non-Chromium, physical AT and aesthetics remain pending.
  - `Drawer` ← beUI `drawer`, `https://beui.dev/r/drawer/raw` and complete registry item `https://beui.dev/r/drawer.json`, retrieved 2026-09-19, MIT. Raw SHA-256 `cb2282d9462850592e6102af210fe7e9e5569788a7d7bef7063d2889a37ee8fb`; registry JSON SHA-256 `e4eaa7294ff9e883a74fd555ff2e41360d47e18f083aa77318cde845cfacf43b`. BEDS adopts the compatible panel utility anatomy plus spring/reduced-motion behavior, preserving the source full-travel `±100%` panel expression while swapping `bg-background`/`shadow-2xl` for BEDS `bg-card`/`--es-shadow-dialog` and preserving the 672px/inline-end geometry. Native `<dialog>`/`::backdrop` replace the upstream aside/backdrop-button pair to preserve browser focus trap/inertness, scroll lock, Escape/backdrop dismissal, long-content scroll region and RTL behavior. Public style/side escape hatches, 320px/85vw geometry and backdrop blur are not adopted. The earlier 2026-09-15 read-only advisory proposing bounded 16/8px travel was not implemented; a later checkpoint superseded it in favor of the beUI/Motion expression. No runtime beUI import or shadcn extraction.
  - `Dialog` native center-morph visual subset ← beUI `center-morph-modal`, `https://beui.dev/r/center-morph-modal/raw` and complete registry item `https://beui.dev/r/center-morph-modal.json`, retrieved 2026-09-19, MIT. Raw SHA-256 `48a20aa11ad2a17678505803c2ff303753b541c3f3722aa7afc0e913d48041df`; registry JSON SHA-256 `b74589ea5aff31a332312dcc89b3e78a49772751bb251a8ad206a06513c1c95e`. BEDS adapts only the standard/welcome center-morph visual subset onto the existing controlled native `<dialog>`: 0.43s `cubic-bezier(.2,0,.2,1)` clip entry and 0.14s reduced-motion opacity. Native lifecycle, focus/inert/Tab/Escape/backdrop/scroll-lock/nested/RTL/trigger-removal semantics remain BEDS-owned; no runtime beUI import, portal lifecycle, duplicate body-scroll owner, public API or `bottom-sheet` API. Full `morphing-modal` remains NO_FIT. Focused browser evidence is `dialog-beui.spec.ts` 5/5 desktop + 5/5 mobile and Dialog control coverage 2/2 desktop + 2/2 mobile; non-Chromium, physical AT and aesthetics remain pending.
  - `Badge` ← beUI `animated-badge`, `https://beui.dev/r/animated-badge/raw` and complete registry item `https://beui.dev/r/animated-badge.json`, retrieved 2026-09-19, MIT. Raw SHA-256 `4d52c2e2be1af023ce911d48121278c94f7bb110913c61ed7d8ade47ee17ea06`; registry JSON SHA-256 `0647ea2c8abd6062cf72d8ab4031e5602cc8f9afb98fdcffec0d5abe37b68f83`. BEDS adopts label/marker roll and layout spring while preserving the `{label,tone,purpose}` API, status-dot geometry and compatibility marker measurement; upstream option/style props and perpetual pulse are not adopted, and reduced motion removes transforms/filters. No runtime beUI import or shadcn extraction.
  - `Disclosure` / `DisclosedRecords` ← beUI `bouncy-accordion`, `https://beui.dev/r/bouncy-accordion/raw`, complete registry item `https://beui.dev/r/bouncy-accordion.json` and documentation `https://beui.dev/components/motion/bouncy-accordion.md`, retrieved 2026-09-19, MIT. Raw SHA-256 `a64ac619939b4fe5167cbb580cb2c9d0714ffb90801050a7e0a19975b28fe9ab`; registry JSON SHA-256 `bee4f7e546db58ed8f1af16c413cee6f26b31a86cb8b5e2cd5d29139075dd7f7`; documentation SHA-256 `ee3ee86088540321e288f20aa801b4796259010acde525901e1d82309fbc73e4`. BEDS adopts the height/opacity/layout and reduced-motion intent in `disclosure.tsx` / `disclosure.css` while preserving measured BEDS APIs and geometry. `DisclosureText` keeps the full string mounted; `LabelField` keeps its hidden remainder mounted inert/aria-hidden; `DisclosedRecords` omits collapsed records from the DOM by deliberate contract. No shadcn command or runtime beUI import. Independent technical review and integrated technical/artifact checks are PASS_WITH_NOTES: focused 10/10 plus existing 12/12, no skips; typechecks PASS. Runtime reduced-motion switching, non-Chromium/physical AT and aesthetics remain pending.
  - `AccountMenu` ← beUI `context-menu`, `https://beui.dev/r/context-menu/raw` and complete registry item `https://beui.dev/r/context-menu.json`, retrieved 2026-09-19, MIT. Raw SHA-256 `5d5f57cf5994803215344285ef8447669870c9bc1017e502d490de4309b28789`; registry JSON SHA-256 `babe5776e5124ae67e28bb741900fae158904b2ee324dc349edddd9b9e07a945`. BEDS keeps the established controlled AccountMenu API, role/dialog semantics, 280px anchored geometry, focus containment/recovery, theme/workspace callbacks and nested modal behavior while adopting compatible active-row/content motion intent. Generic context-menu trigger/menuitem/checkbox/radio APIs and public style/size escapes are not exposed. No runtime beUI import or shadcn extraction. Independent technical review and integrated technical/artifact checks are PASS_WITH_NOTES: 30 passed / 6 intentional conditional skips / 0 failed across AccountMenu and shell; skips intentional. Physical AT, non-Chromium and aesthetics remain pending.
  - `CommandPalette` ← beUI `command-palette`, `https://beui.dev/r/command-palette/raw`, complete registry item `https://beui.dev/r/command-palette.json` and MIT repository `https://github.com/starc007/ui-components`, retrieved 2026-09-19. Raw SHA-256 `fa19172779923d319155166a71038e7077dd9f71a056591ae9493c0ec9ad05f7`; registry JSON SHA-256 `c58fed6d7f19dd6e6c5ab9202477b0751539ace1b6f929fae90e210606dac175`; repository main HEAD `90c29d7f80f661263f7b424629738e40a5a48db7`, LICENSE SHA-256 `9e27b491d5691a1bd95708e79de23beb2962f33fe6c1ee31ab639f43009a0b20`. BEDS adopts fuzzy search/cursor, active-row motion and reduced-motion intent in `command-palette.tsx` while preserving the controlled API and measured native-dialog geometry. Native modal lifecycle is shared through `lib/modal.ts` with one document scroll-lock WeakMap. PresenceGate and panel entrance motion are intentionally omitted to avoid a second exit/scroll-lock state machine and transform-dependent outside-click bounds. No public grouped/keyword/badge/checkbox/radio APIs, style escapes, shadcn command or runtime beUI import. Independent technical review and integrated technical/artifact checks are PASS_WITH_NOTES: focused 8/8 plus existing command-search 4/4; serial Drawer/SearchDialog regression 20/20, no skips/failures. Non-Chromium/physical AT and aesthetics remain pending.
  - `Select` / `FilterSelect` ← beUI `select`, `https://beui.dev/r/select/raw` and complete registry item `https://beui.dev/r/select.json`, retrieved 2026-09-19, MIT. Raw SHA-256 `2752510f98d60caea18794d93f868916dbf27f099746828ca9e5bde8c4a201ab`; registry JSON SHA-256 `6c728098323f93c3ec7e713c5cbfc2668dbd12ee24b93ad9bd788d6720b6ba6b`. BEDS adopts bounded trigger-radius, chevron, popup-opacity and option-entry motion in `select.tsx` while preserving its monolithic controlled API, native manual popover, fixed 260px geometry, viewport clamp, disabled skipping, `aria-activedescendant`, keyboard navigation and focus restoration. Shared `lib/anchored-popup.ts` and `lib/option-navigation.ts` retain the existing overlay/navigation behavior without duplication. The composable source API, trigger-width panel, search and multi-select behavior are not adopted. No runtime beUI import or shadcn extraction. Independent technical review and integrated `npm run verify`/extracted-artifact consumer smoke are PASS_WITH_NOTES: serial Select and regression coverage totaled 40/40 with 0 skips/failures across desktop/mobile and light/dark. Non-Chromium/physical AT and aesthetics remain pending.
  - `AppShell` navigation anatomy ← beUI `animated-sidebar` and `bounce-sidebar`, `https://beui.dev/r/animated-sidebar/raw`, `https://beui.dev/r/bounce-sidebar/raw`, retrieved 2026-09-19, MIT. Raw SHA-256 values are `e78473dbbb235a856fa1696dce81ee7663b14ae0cb22e5f795bb96551f987a0c` and `62f577208fde0dd7317d96f6b996019ba72d4b2c416ceba361fa0c4bece061c5`; registry JSON SHA-256 values are `e821e9fe78f217d569701d85c521611f88b196795f521c9272784a0e30702cf2` and `08b7f9778226417cd3d9bf0eb1618ee4e2a620953261f4ecf75e3b6201a63ad3`. BEDS adapts only the reduced-motion guarded shell grid interpolation, scoped active-row layout indicator and spring intent. Public provider/composable APIs, source widths, keyboard shortcut, portal dialog and item model are not exposed; BEDS preserves 264px/62px, native mobile drawer, focus/inert/skip-link, logical RTL and locked-item contracts. No runtime beUI import.
  - Evaluated without adoption: beUI `morphing-search` (`https://beui.dev/r/morphing-search/raw`, raw SHA-256 `e7b5f55159f78d83df0754705980b04784ad7214f92fb87d3bcc82102729996d`, registry JSON SHA-256 `959dcdab4757566087e075bba41cdaf0c613e8be247170cf08d188e53ae173d8`), `not-found` (raw SHA-256 `df94905c8d6bbf61e8b1bc02382d62f05aeb9fe84c21c18e3acdd3c0d024ea1d`; registry endpoint 404), `cylinder-carousel` (raw SHA-256 `2a0ee658595e356985ae0b76009201506f74c0f7e7675c1de2a32e5ada57d8d3`; registry JSON SHA-256 `fd905d596940706df4d140dcfba6ac8af564418e1bb66a2f111a640eccba9ba0`), `marquee` (raw SHA-256 `d8d1d371d0d2c800e4ddfd8293f1141b625f084ff7d9b12ca108c37d0b1512d4`; registry JSON SHA-256 `41f83bb7a912ac4fe7d2b19e203d9e8383eae92bfb1dc50f48571e5164e25c82`), `popover` (raw SHA-256 `79ec5103702ab4c5d4802b6533b8b73219cf1695c4461ac63a60a4a8e60ae8ef`; registry JSON SHA-256 `5c0c3f5141c136f19d5ef6248189a7521fcd26f033a857620f2e6482bc6d73db`) and `table` (raw SHA-256 `7496bd8522412b5ec050a41b47d493a4292a5a49919b2b9fbbd591e4dbca543f`; registry JSON SHA-256 `45f58d7dcb5528877fc4581aa69d363a22f123f4e58ac60dd89ff604fc4b4904`), all retrieved 2026-09-19 under MIT. They remain NO_FIT or DEFERRED_REQUIREMENT as recorded in provenance; no runtime source or new public capability is included. No matching free raw slug resolved for `notice`, `skeleton` or `avatar` (404), so no third-party notice is claimed for those local contracts.
- `motion` (Framer Motion), MIT: runtime animation dependency approved by the design owner on 2026-09-16 and declared in `packages/beds/package.json`.
- Tailwind CSS v4, MIT: build-time dependency only. Utilities are compiled into `dist/styles.css`; consumers never install or import Tailwind.
- `clsx` (MIT) and `tailwind-merge` (MIT): runtime dependencies composing the internal `cn` helper (`packages/beds/src/lib/utils.ts`), never exported.
- `embla-carousel-react` (MIT): runtime dependency behind the paged carousel primitive.
- `input-otp` (MIT): runtime dependency behind the OTP input primitive.
- Reference screenshots, marks and third-party product names document design research; their rights remain with their owners. They are not a grant to use those brands in consumer products.

No new open-source license is assigned to project-owned code by this extraction.
Public visibility and GitHub download availability are not a relicensing decision.
